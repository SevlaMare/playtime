import {
  display,
  spacing,
  typography,
  shadow,
  color,
  image,
  container,
  effect
} from "./Constants";

const styles = {
  ...display,
  ...spacing,
  ...typography,
  ...shadow,
  ...color,
  ...image,
  ...container,
  ...effect
}

export default styles;